import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import "leaflet/dist/leaflet.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Maps to Any - Share locations that open in any app",
  description: "Convert Google Maps links into shareable URLs that open in any navigation app. Just like WhatsApp location sharing!",
  keywords: ["maps", "navigation", "uber", "waze", "google maps", "apple maps", "location sharing"],
  openGraph: {
    title: "Maps to Any",
    description: "Share locations that open in any navigation app",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
